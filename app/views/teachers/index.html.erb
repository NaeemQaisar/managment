<h1>Listing teachers</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Fathername</th>
    <th>Designation</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @teachers.each do |teacher| %>
  <tr>
    <td><%= teacher.name %></td>
    <td><%= teacher.fathername %></td>
    <td><%= teacher.designation %></td>
    <td><%= link_to 'Show', classroom_teacher_path %></td>
    <td><%= link_to 'Edit', edit_classroom_teacher_path(classroom.id) %></td>
    <td><%= link_to 'Destroy', teacher, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Teacher', new_classroom_teacher_path %>
