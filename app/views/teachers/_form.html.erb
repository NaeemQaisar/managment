<%= form_for(@classroom,@teacher) do |f| %>
  <% if @teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teacher.errors.count, "error") %> prohibited this teacher from being saved:</h2>

      <ul>
      <% @teacher.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :fathername %><br />
    <%= f.text_field :fathername %>
  </div>
  <%= f.label :students %>
  <%= select_student "student", options_from_collection_for_select(Student.all, 'id', 'name', @student.teachers.map{ |j| j.id}), :multiple => true %>
  <div class="field">
    <%= f.label :designation %><br />
    <%= f.text_area :designation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
